### Phonebook part3

Application address: https://helsinki-open-fullstack.onrender.com

### Notes

- Used Render UI to set up
- Change `root-directory` in Settings to `part3/phonebook`
- Change `build-command` to `npm run build`
- Change `start-command` to `npm run start`
- Free instance (0.15 vCPU + 512MB RAM) has a start-up time (~15-30 seconds)